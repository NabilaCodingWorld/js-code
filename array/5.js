// Combining Arrays
// Instructions:

// Create two arrays of your choice.
// Use the concat method to combine the two arrays into a new array.
// Print both the original arrays and the combined array using console.log().

const food1 = ["Rice", "Lentil", "Tomato"];
console.log(food1);
const food2 = ["Barger", "Pizza", "Suchi"];
console.log(food2);
const allFood = food1.concat(food2);
console.log(allFood);
